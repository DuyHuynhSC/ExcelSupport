<Window x:Class="ExcelSupport.Views.OracleQuickQueryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:loc="clr-namespace:ExcelSupport.Helpers"
        mc:Ignorable="d"
        Title="{loc:Loc Oracle_QuickQueryTitle}"
        Height="520" Width="780"
        MinHeight="440" MinWidth="700"
        WindowStartupLocation="CenterScreen"
        Background="#F8FAFC"
        FontFamily="Segoe UI"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- Form Input Style -->
        <Style x:Key="QueryInputStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Padding="{TemplateBinding Padding}" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2563EB"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- SQL Editor Style -->
        <Style x:Key="SqlEditorStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Foreground" Value="#0F172A"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontFamily" Value="Consolas, 'Cascadia Mono', 'Courier New'"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="AcceptsTab" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2563EB"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="1.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Buttons -->
        <Style x:Key="PrimaryActionBtn" TargetType="Button">
            <Setter Property="Background" Value="#16A34A"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#15803D"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="#94A3B8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryActionBtn" TargetType="Button">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#E2E8F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- 0. Header -->
            <RowDefinition Height="*"/>    <!-- 1. Form Content -->
            <RowDefinition Height="Auto"/> <!-- 2. Footer Bar -->
        </Grid.RowDefinitions>

        <!-- ==================== 0. HEADER ==================== -->
        <Border Grid.Row="0" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="⚡" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{loc:Loc Oracle_QuickQueryHeader}" FontWeight="Bold" FontSize="14.5" Foreground="#0F172A"/>
                    </StackPanel>
                    <TextBlock Text="{loc:Loc Oracle_QuickQuerySubHeader}" FontSize="11.5" Foreground="#64748B" TextWrapping="Wrap" Margin="26,3,0,0"/>
                </StackPanel>

                <!-- Shortcut Badge -->
                <Border Grid.Column="1" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="4" Padding="10,6" VerticalAlignment="Center">
                    <TextBlock Text="Ctrl + Enter: Thực thi" FontSize="11" FontWeight="SemiBold" Foreground="#2563EB"/>
                </Border>
            </Grid>
        </Border>

        <!-- ==================== 1. BODY CONTENT ==================== -->
        <Grid Grid.Row="1" Margin="20,14,20,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Connection & Target Row -->
                <RowDefinition Height="*"/>    <!-- SQL Text Editor -->
                <RowDefinition Height="Auto"/> <!-- Options Row -->
            </Grid.RowDefinitions>

            <!-- Top Row: Connection profile & Target cell -->
            <Grid Grid.Row="0" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="260"/>
                </Grid.ColumnDefinitions>

                <!-- Connection Selector -->
                <StackPanel Grid.Column="0">
                    <TextBlock Text="{loc:Loc Oracle_SavedProfile}" FontSize="11.5" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,4"/>
                    <ComboBox x:Name="cboProfile" Height="30" FontSize="12" DisplayMemberPath="Name" SelectionChanged="CboProfile_SelectionChanged"/>
                </StackPanel>

                <!-- Target Location -->
                <StackPanel Grid.Column="2">
                    <TextBlock Text="{loc:Loc Oracle_InsertAt}" FontSize="11.5" FontWeight="SemiBold" Foreground="#475569" Margin="0,0,0,4"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="txtTargetLocation" Height="30" Style="{StaticResource QueryInputStyle}" FontWeight="SemiBold" ToolTip="{loc:Loc Oracle_InsertLocationTip}"/>
                        <Button Grid.Column="1" x:Name="btnPickLocation" Style="{StaticResource SecondaryActionBtn}" Padding="8,4" Margin="4,0,0,0" Height="30" Click="BtnPickLocation_Click" ToolTip="{loc:Loc Oracle_BtnPickLocationTip}">
                            <TextBlock Text="🎯" FontSize="12"/>
                        </Button>
                        <Button Grid.Column="2" x:Name="btnRefreshLocation" Style="{StaticResource SecondaryActionBtn}" Padding="8,4" Margin="2,0,0,0" Height="30" Click="BtnRefreshLocation_Click" ToolTip="{loc:Loc Oracle_BtnRefreshLocationTip}">
                            <TextBlock Text="🔄" FontSize="12"/>
                        </Button>
                    </Grid>
                </StackPanel>
            </Grid>

            <!-- Middle: SQL Editor -->
            <Grid Grid.Row="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,4">
                    <TextBlock Text="{loc:Loc Oracle_SqlEditorLabel}" FontSize="11.5" FontWeight="SemiBold" Foreground="#475569"/>
                    <TextBlock Text=" (vd: SELECT * FROM ADDMTA WHERE ROWNUM &lt;= 50)" FontSize="11" Foreground="#94A3B8" Margin="6,0,0,0"/>
                </StackPanel>

                <TextBox Grid.Row="1" x:Name="txtSqlQuery" Style="{StaticResource SqlEditorStyle}"/>
            </Grid>

            <!-- Bottom: Options Checkboxes -->
            <Border Grid.Row="2" Background="#F1F5F9" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" Padding="14,10" Margin="0,10,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <CheckBox x:Name="chkIncludeTitle" Content="{loc:Loc Oracle_OptIncludeTitle}" IsChecked="True" FontSize="11.5" Margin="0,0,18,0" VerticalAlignment="Center"/>
                        <CheckBox x:Name="chkIncludeHeaders" Content="{loc:Loc Oracle_OptIncludeHeaders}" IsChecked="True" FontSize="11.5" Margin="0,0,18,0" VerticalAlignment="Center"/>
                        <CheckBox x:Name="chkAutoFit" Content="{loc:Loc Oracle_OptAutoFit}" IsChecked="True" FontSize="11.5" Margin="0,0,18,0" VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                        <TextBlock Text="{loc:Loc Oracle_LimitRows}" FontSize="11.5" Foreground="#475569" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBox x:Name="txtMaxRows" Text="0" Width="60" Height="30" TextAlignment="Center" Style="{StaticResource QueryInputStyle}" ToolTip="{loc:Loc Oracle_LimitRowsTip}"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ==================== 2. FOOTER BAR ==================== -->
        <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#E2E8F0" BorderThickness="0,1,0,0" Padding="20,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Status Text & Progress -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="txtStatus" Text="{loc:Loc Oracle_StatusReady}" FontSize="11.5" Foreground="#475569" VerticalAlignment="Center"/>
                    <ProgressBar x:Name="pbProgress" Height="12" Width="140" Margin="10,0,0,0" IsIndeterminate="True" Visibility="Collapsed"/>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button x:Name="btnExecute" Style="{StaticResource PrimaryActionBtn}" Click="BtnExecute_Click" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="▶" Margin="0,0,6,0"/>
                            <TextBlock Text="{loc:Loc Oracle_BtnExecuteInsert}"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="btnCancel" Content="{loc:Loc Common_Close}" Style="{StaticResource SecondaryActionBtn}" Click="BtnCancel_Click" Width="85"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
