<Window x:Class="ExcelSupport.Views.DesignPageCounterDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:loc="clr-namespace:ExcelSupport.Helpers"
        mc:Ignorable="d"
        Title="{loc:Loc PageCounter_Title}"
        Height="730" Width="1020"
        MinHeight="600" MinWidth="920"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        KeyDown="Window_KeyDown">

    <Window.Style>
        <Style TargetType="Window">
            <Setter Property="Background" Value="#F8FAFC"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource Self}}" Value="True">
                    <Setter Property="Background" Value="#0F172A"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Style>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>

        <!-- Primary Action Button -->
        <Style x:Key="PrimaryActionBtn" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="#94A3B8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Success Action Button -->
        <Style x:Key="SuccessActionBtn" TargetType="Button">
            <Setter Property="Background" Value="#16A34A"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#15803D"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="#94A3B8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Evidence Action Button (Purple/Violet) -->
        <Style x:Key="EvidenceActionBtn" TargetType="Button">
            <Setter Property="Background" Value="#7C3AED"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#6D28D9"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="bd" Property="Background" Value="#94A3B8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Secondary Action Button -->
        <Style x:Key="SecondaryActionBtn" TargetType="Button">
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#E2E8F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="False">
                    <Setter Property="Background" Value="#F1F5F9"/>
                    <Setter Property="Foreground" Value="#334155"/>
                    <Setter Property="BorderBrush" Value="#CBD5E1"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                    <Setter Property="Background" Value="#334155"/>
                    <Setter Property="Foreground" Value="#F8FAFC"/>
                    <Setter Property="BorderBrush" Value="#475569"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- ComboBox Style -->
        <Style TargetType="ComboBox">
            <Setter Property="MinHeight" Value="28"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                    <Setter Property="Foreground" Value="#F8FAFC"/>
                    <Setter Property="Background" Value="#1E293B"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- CheckBox Style -->
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                    <Setter Property="Foreground" Value="#F8FAFC"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- RadioButton Style -->
        <Style TargetType="RadioButton">
            <Setter Property="Foreground" Value="#1E293B"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                    <Setter Property="Foreground" Value="#F8FAFC"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid Style -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E2E8F0"/>
            <Setter Property="RowBackground" Value="#FFFFFF"/>
            <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#E2E8F0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="RowHeight" Value="28"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                    <Setter Property="Background" Value="#1E293B"/>
                    <Setter Property="BorderBrush" Value="#334155"/>
                    <Setter Property="RowBackground" Value="#1E293B"/>
                    <Setter Property="AlternatingRowBackground" Value="#0F172A"/>
                    <Setter Property="HorizontalGridLinesBrush" Value="#334155"/>
                    <Setter Property="Foreground" Value="#F8FAFC"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F1F5F9"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                    <Setter Property="Background" Value="#334155"/>
                    <Setter Property="Foreground" Value="#F8FAFC"/>
                    <Setter Property="BorderBrush" Value="#475569"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Expander Style -->
        <Style TargetType="Expander">
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                    <Setter Property="Foreground" Value="#F8FAFC"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- 0. Header -->
            <RowDefinition Height="Auto"/> <!-- 1. Source & Options Configuration -->
            <RowDefinition Height="*"/>    <!-- 2. Results Dashboard -->
            <RowDefinition Height="Auto"/> <!-- 3. Footer Bar -->
        </Grid.RowDefinitions>

        <!-- ==================== 0. HEADER ==================== -->
        <Border Grid.Row="0" BorderThickness="0,0,0,1" Padding="20,12">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#FFFFFF"/>
                    <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                            <Setter Property="Background" Value="#1E293B"/>
                            <Setter Property="BorderBrush" Value="#334155"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="📑" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{loc:Loc PageCounter_Header}" FontWeight="Bold" FontSize="14.5">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#0F172A"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                            <Setter Property="Foreground" Value="#F8FAFC"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                    <TextBlock Text="{loc:Loc PageCounter_SubHeader}" FontSize="11.5" TextWrapping="Wrap" Margin="26,3,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#64748B"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                        <Setter Property="Foreground" Value="#94A3B8"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </StackPanel>

                <!-- Badge -->
                <Border Grid.Column="1" BorderThickness="1" CornerRadius="4" Padding="10,6" VerticalAlignment="Center">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="#EFF6FF"/>
                            <Setter Property="BorderBrush" Value="#BFDBFE"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                    <Setter Property="Background" Value="#1E3A8A"/>
                                    <Setter Property="BorderBrush" Value="#3B82F6"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="🎯 Template Diff Engine" FontSize="11" FontWeight="SemiBold">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#2563EB"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                        <Setter Property="Foreground" Value="#93C5FD"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Border>
            </Grid>
        </Border>

        <!-- ==================== 1. SOURCE & OPTIONS CONFIGURATION ==================== -->
        <Border Grid.Row="1" BorderThickness="0,0,0,1" Padding="20,12">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#F8FAFC"/>
                    <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                            <Setter Property="Background" Value="#0F172A"/>
                            <Setter Property="BorderBrush" Value="#334155"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Row 0: File selectors -->
                    <RowDefinition Height="Auto"/> <!-- Row 1: Mode & Density / Highlight Options -->
                    <RowDefinition Height="Auto"/> <!-- Row 2: General Checkboxes & Action Buttons -->
                    <RowDefinition Height="Auto"/> <!-- Row 3: Sheet Selection Expander -->
                </Grid.RowDefinitions>

                <!-- Row 0: Target & Template Selector -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Target File -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="{loc:Loc PageCounter_TargetFileLabel}" FontWeight="SemiBold" FontSize="11.5" Margin="0,0,0,4">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#334155"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                            <Setter Property="Foreground" Value="#CBD5E1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox ItemsSource="{Binding OpenWorkbooks}" SelectedItem="{Binding SelectedTargetWorkbook, Mode=TwoWay}" Height="30" ToolTip="{Binding SelectedTargetWorkbook}"/>
                            <Button Grid.Column="1" Command="{Binding BrowseTargetFileCommand}" Style="{StaticResource SecondaryActionBtn}" Margin="4,0,0,0" Padding="8,4" Height="30" ToolTip="{loc:Loc PageCounter_BrowseTip}">
                                <TextBlock Text="📁" FontSize="12"/>
                            </Button>
                            <Button Grid.Column="2" Command="{Binding CreateNewCopyForHighlightCommand}" Style="{StaticResource SecondaryActionBtn}" Margin="4,0,0,0" Padding="8,4" Height="30" ToolTip="{loc:Loc PageCounter_BtnCreateNewCopyTip}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📝" FontSize="12" Margin="0,0,4,0"/>
                                    <TextBlock Text="{loc:Loc PageCounter_BtnCreateNewCopy}" FontSize="11" FontWeight="Medium"/>
                                </StackPanel>
                            </Button>
                        </Grid>
                    </StackPanel>

                    <!-- Template File -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="{loc:Loc PageCounter_TemplateFileLabel}" FontWeight="SemiBold" FontSize="11.5" Margin="0,0,0,4">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="#334155"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                            <Setter Property="Foreground" Value="#CBD5E1"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ComboBox ItemsSource="{Binding OpenWorkbooks}" SelectedItem="{Binding SelectedTemplateWorkbook, Mode=TwoWay}" Height="30" ToolTip="{Binding SelectedTemplateWorkbook}"/>
                            <Button Grid.Column="1" Command="{Binding BrowseTemplateFileCommand}" Style="{StaticResource SecondaryActionBtn}" Margin="4,0,0,0" Padding="8,4" Height="30" ToolTip="{loc:Loc PageCounter_BrowseTip}">
                                <TextBlock Text="📁" FontSize="12"/>
                            </Button>
                        </Grid>
                    </StackPanel>
                </Grid>

                <!-- Row 1: Algorithm Mode & Density / Highlight Parameters -->
                <Border Grid.Row="1" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Padding="10,8" Margin="0,0,0,10">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                    <Setter Property="Background" Value="#1E293B"/>
                                    <Setter Property="BorderBrush" Value="#334155"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Algorithm Mode RadioButtons -->
                        <WrapPanel Grid.Row="0" Margin="0,0,0,8" VerticalAlignment="Center">
                            <RadioButton Content="{loc:Loc PageCounter_ModeUserHighlight}" IsChecked="{Binding IsUserHighlightMode, Mode=TwoWay}" FontWeight="SemiBold" Margin="0,0,18,0"/>
                            <RadioButton Content="{loc:Loc PageCounter_ModeAutoDiff}" IsChecked="{Binding IsAutoDiffMode, Mode=TwoWay}" Margin="0,0,18,0"/>
                            <RadioButton Content="{loc:Loc PageCounter_ModePrintBreak}" IsChecked="{Binding IsPrintBreakMode, Mode=TwoWay}"/>
                        </WrapPanel>

                        <!-- Character & Highlight Specific Options -->
                        <WrapPanel Grid.Row="1" VerticalAlignment="Center" Visibility="{Binding IsCharHighlightMode, Converter={StaticResource BoolToVisConverter}}">
                            <!-- Preset Profile -->
                            <StackPanel Orientation="Horizontal" Margin="0,0,16,6" VerticalAlignment="Center">
                                <TextBlock Text="{loc:Loc PageCounter_ProfileLabel}" FontSize="11.5" FontWeight="SemiBold" VerticalAlignment="Center" Margin="0,0,6,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#1D4ED8"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                                    <Setter Property="Foreground" Value="#60A5FA"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <ComboBox ItemsSource="{Binding AvailableProfiles}" SelectedItem="{Binding SelectedProfile, Mode=TwoWay}" DisplayMemberPath="Name" MinWidth="230" Height="28" ToolTip="{Binding SelectedProfile.Description}"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,16,6" VerticalAlignment="Center">
                                <TextBlock Text="{loc:Loc PageCounter_DensityLabel}" FontSize="11.5" FontWeight="Medium" VerticalAlignment="Center" Margin="0,0,6,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#475569"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                                    <Setter Property="Foreground" Value="#CBD5E1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <ComboBox ItemsSource="{Binding AvailableDensityPresets}" SelectedItem="{Binding SelectedDensityPreset, Mode=TwoWay}" DisplayMemberPath="Name" MinWidth="260" Height="28"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,16,6" VerticalAlignment="Center">
                                <TextBlock Text="{loc:Loc PageCounter_HighlightColorLabel}" FontSize="11.5" FontWeight="Medium" VerticalAlignment="Center" Margin="0,0,6,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#475569"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                                    <Setter Property="Foreground" Value="#CBD5E1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <ComboBox ItemsSource="{Binding AvailableHighlightColors}" SelectedItem="{Binding SelectedHighlightColor, Mode=TwoWay}" MinWidth="220" Height="28" ToolTip="{loc:Loc PageCounter_ColorShortcutTip}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                                <Border Width="16" Height="16" CornerRadius="3" Background="{Binding SwatchBrush}" BorderBrush="#94A3B8" BorderThickness="1" Margin="0,0,7,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontSize="11.5"/>
                                            </StackPanel>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                                <Button Command="{Binding HighlightSelectionCommand}" Style="{StaticResource SecondaryActionBtn}" Padding="8,4" Height="28" Margin="6,0,0,0" VerticalAlignment="Center" ToolTip="{loc:Loc PageCounter_BtnHighlightSelectionTip}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="🎨" Margin="0,0,4,0" FontSize="11" VerticalAlignment="Center"/>
                                        <TextBlock Text="{loc:Loc PageCounter_BtnHighlightSelection}" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        <Border Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="3" Padding="4,1" Margin="6,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="Ctrl+Shift+H" FontSize="9.5" FontWeight="Bold" Foreground="#2563EB"/>
                                        </Border>
                                    </StackPanel>
                                </Button>
                                <Button Command="{Binding ClearHighlightSelectionCommand}" Style="{StaticResource SecondaryActionBtn}" Padding="8,4" Height="28" Margin="6,0,0,0" VerticalAlignment="Center" ToolTip="{loc:Loc PageCounter_BtnClearHighlightSelectionTip}">
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <TextBlock Text="🧹" Margin="0,0,4,0" FontSize="11" VerticalAlignment="Center"/>
                                        <TextBlock Text="{loc:Loc PageCounter_BtnClearHighlightSelection}" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                        <Border Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="1" CornerRadius="3" Padding="4,1" Margin="6,0,0,0" VerticalAlignment="Center">
                                            <TextBlock Text="Ctrl+Shift+Alt+H" FontSize="9.5" FontWeight="Bold" Foreground="#DC2626"/>
                                        </Border>
                                    </StackPanel>
                                </Button>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="0,0,16,6" VerticalAlignment="Center">
                                <TextBlock Text="{loc:Loc PageCounter_ShapeFactorLabel}" FontSize="11.5" FontWeight="Medium" VerticalAlignment="Center" Margin="0,0,6,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#475569"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                                    <Setter Property="Foreground" Value="#CBD5E1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <ComboBox ItemsSource="{Binding AvailableShapeFactors}" SelectedItem="{Binding SelectedShapeFactorPreset, Mode=TwoWay}" DisplayMemberPath="Name" MinWidth="220" Height="28"/>
                            </StackPanel>

                            <CheckBox Content="{loc:Loc PageCounter_OptCreateEvidence}" IsChecked="{Binding HighlightChangedCells, Mode=TwoWay}" Margin="0,0,0,6" FontWeight="Medium" Visibility="{Binding IsAutoDiffMode, Converter={StaticResource BoolToVisConverter}}"/>
                        </WrapPanel>
                    </Grid>
                </Border>

                <!-- Row 2: General Options & Action Button -->
                <Grid Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <WrapPanel Grid.Column="0" VerticalAlignment="Center">
                        <CheckBox Content="{loc:Loc PageCounter_OptIgnoreCover}" IsChecked="{Binding IgnoreCoverAndHistory, Mode=TwoWay}" Margin="0,0,16,0"/>
                        <CheckBox Content="{loc:Loc PageCounter_OptIgnoreBlank}" IsChecked="{Binding IgnoreBlankPages, Mode=TwoWay}" Margin="0,0,16,0"/>
                        <CheckBox Content="{loc:Loc PageCounter_OptCountShapes}" IsChecked="{Binding CountShapesAndPictures, Mode=TwoWay}" Margin="0,0,16,0"/>
                    </WrapPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Command="{Binding RefreshWorkbooksCommand}" Style="{StaticResource SecondaryActionBtn}" Padding="8,4" Margin="0,0,8,0" Height="32" ToolTip="{loc:Loc PageCounter_RefreshTip}">
                            <TextBlock Text="🔄" FontSize="12"/>
                        </Button>
                        <Button Command="{Binding AnalyzeCommand}" Style="{StaticResource PrimaryActionBtn}" Height="32" Padding="16,6">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔍" Margin="0,0,6,0"/>
                                <TextBlock Text="{loc:Loc PageCounter_BtnAnalyze}"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- Row 3: Sheet Inclusion / Exclusion Section -->
                <Expander Grid.Row="3" Margin="0,10,0,0" IsExpanded="False">
                    <Expander.Header>
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock Text="📑" Margin="0,0,6,0" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding SheetSelectionHeader}" FontWeight="SemiBold" FontSize="11.5" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="#334155"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                                <Setter Property="Foreground" Value="#E2E8F0"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Expander.Header>
                    <Border BorderThickness="1" CornerRadius="6" Padding="10,8" Margin="0,6,0,0">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#FFFFFF"/>
                                <Setter Property="BorderBrush" Value="#E2E8F0"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                        <Setter Property="Background" Value="#1E293B"/>
                                        <Setter Property="BorderBrush" Value="#334155"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/> <!-- Quick Action Buttons -->
                                <RowDefinition Height="Auto"/> <!-- Sheets WrapPanel / List -->
                            </Grid.RowDefinitions>

                            <!-- Quick Action Buttons -->
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                                <TextBlock Text="{loc:Loc PageCounter_SheetSelectTip}" FontSize="11" Foreground="#64748B" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <Button Content="{loc:Loc PageCounter_BtnSelectAll}" Command="{Binding SelectAllSheetsCommand}" Style="{StaticResource SecondaryActionBtn}" Padding="8,3" FontSize="11" Margin="0,0,6,0"/>
                                <Button Content="{loc:Loc PageCounter_BtnDeselectAll}" Command="{Binding DeselectAllSheetsCommand}" Style="{StaticResource SecondaryActionBtn}" Padding="8,3" FontSize="11" Margin="0,0,6,0"/>
                                <Button Content="{loc:Loc PageCounter_BtnInvertSelect}" Command="{Binding InvertSheetsSelectionCommand}" Style="{StaticResource SecondaryActionBtn}" Padding="8,3" FontSize="11"/>
                            </StackPanel>

                            <!-- Sheets WrapPanel in ScrollViewer -->
                            <ScrollViewer Grid.Row="1" MaxHeight="120" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding AvailableSheets}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderThickness="1" CornerRadius="4" Padding="6,3" Margin="0,0,8,6">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#F8FAFC"/>
                                                        <Setter Property="BorderBrush" Value="#CBD5E1"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsIncluded}" Value="True">
                                                                <Setter Property="Background" Value="#EFF6FF"/>
                                                                <Setter Property="BorderBrush" Value="#93C5FD"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                                                <Setter Property="Background" Value="#0F172A"/>
                                                                <Setter Property="BorderBrush" Value="#334155"/>
                                                            </DataTrigger>
                                                            <MultiDataTrigger>
                                                                <MultiDataTrigger.Conditions>
                                                                    <Condition Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True"/>
                                                                    <Condition Binding="{Binding IsIncluded}" Value="True"/>
                                                                </MultiDataTrigger.Conditions>
                                                                <Setter Property="Background" Value="#1E3A8A"/>
                                                                <Setter Property="BorderBrush" Value="#3B82F6"/>
                                                            </MultiDataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <CheckBox IsChecked="{Binding IsIncluded, Mode=TwoWay}" Content="{Binding SheetName}" FontSize="11.5" FontWeight="Medium"/>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Expander>
            </Grid>
        </Border>

        <!-- ==================== 2. RESULTS DASHBOARD ==================== -->
        <Grid Grid.Row="2" Margin="20,14">
            <!-- Progress Bar Overlay when analyzing -->
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Visibility="{Binding IsAnalyzing, Converter={StaticResource BoolToVisConverter}}">
                <StackPanel Width="320" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding ProgressMessage}" TextAlignment="Center" FontSize="12" Margin="0,0,0,8">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#475569"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                        <Setter Property="Foreground" Value="#F8FAFC"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <ProgressBar Height="14" Value="{Binding ProgressPercentage}" Minimum="0" Maximum="100"/>
                </StackPanel>
            </Grid>

            <!-- Results View -->
            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasResult}" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Summary KPI Cards -->
                    <RowDefinition Height="*"/>    <!-- Split View (Sheets Table + Page Breakdown) -->
                </Grid.RowDefinitions>

                <!-- Summary KPI Cards -->
                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Card 1: Total Changed Characters -->
                    <Border Grid.Column="0" Background="#EFF6FF" BorderBrush="#BFDBFE" BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc PageCounter_CardCharsCount}" FontSize="11" Foreground="#1E40AF" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Result.TotalChangedCharacters, StringFormat={}{0:N0}}" FontSize="20" FontWeight="Bold" Foreground="#1D4ED8" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Card 2: New Shapes / Diagrams -->
                    <Border Grid.Column="1" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc PageCounter_CardShapesCount}" FontSize="11" Foreground="#475569" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Result.TotalAddedShapes}" FontSize="20" FontWeight="Bold" Foreground="#334155" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Card 3: Net Work Pages -->
                    <Border Grid.Column="2" Background="#F0FDF4" BorderBrush="#BBF7D0" BorderThickness="1" CornerRadius="6" Padding="12,8" Margin="0,0,8,0">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc PageCounter_CardWorkPages}" FontSize="11" Foreground="#166534" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding Result.TotalWorkPages, StringFormat={}{0:F1}}" FontSize="20" FontWeight="Bold" Foreground="#15803D" Margin="0,2,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Card 4: Effort / Work Percent -->
                    <Border Grid.Column="3" Background="#FEF2F2" BorderBrush="#FECACA" BorderThickness="1" CornerRadius="6" Padding="12,8">
                        <StackPanel>
                            <TextBlock Text="{loc:Loc PageCounter_CardWorkPercent}" FontSize="11" Foreground="#991B1B" FontWeight="SemiBold"/>
                            <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                <TextBlock Text="{Binding Result.OverallWorkPercent}" FontSize="20" FontWeight="Bold" Foreground="#DC2626"/>
                                <TextBlock Text="%" FontSize="14" FontWeight="Bold" Foreground="#DC2626" VerticalAlignment="Bottom" Margin="2,0,0,2"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Split View: Left Table of Sheets, Right Page Breakdown List -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="10"/>
                        <ColumnDefinition Width="320"/>
                    </Grid.ColumnDefinitions>

                    <!-- Left: DataGrid of Sheets -->
                    <Border Grid.Column="0" BorderThickness="1" CornerRadius="6">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="BorderBrush" Value="#E2E8F0"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                        <Setter Property="BorderBrush" Value="#334155"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <DataGrid ItemsSource="{Binding Result.SheetResults}" SelectedItem="{Binding SelectedSheetResult, Mode=TwoWay}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single" HeadersVisibility="Column">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="{loc:Loc PageCounter_ColSheetName}" Binding="{Binding SheetName}" Width="*" FontWeight="SemiBold"/>
                                <DataGridTextColumn Header="{loc:Loc PageCounter_ColStatus}" Binding="{Binding SummaryText}" Width="150"/>
                                <DataGridTextColumn Header="{loc:Loc PageCounter_ColTotalPages}" Binding="{Binding TotalPrintPages}" Width="70"/>
                                <DataGridTextColumn Header="{loc:Loc PageCounter_ColChars}" Binding="{Binding TotalChangedCharacters, StringFormat={}{0:N0}}" Width="80"/>
                                <DataGridTextColumn Header="{loc:Loc PageCounter_ColShapes}" Binding="{Binding TotalAddedShapes}" Width="65"/>
                                <DataGridTextColumn Header="{loc:Loc PageCounter_ColWorkPages}" Binding="{Binding WorkPagesCount, StringFormat={}{0:F1}}" Width="80"/>
                                <DataGridTextColumn Header="{loc:Loc PageCounter_ColPercent}" Binding="{Binding WorkPercent, StringFormat={}{0}%}" Width="70"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!-- Right: Selected Sheet Detail Page List -->
                    <Border Grid.Column="2" BorderThickness="1" CornerRadius="6" Padding="12,10">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="#FFFFFF"/>
                                <Setter Property="BorderBrush" Value="#E2E8F0"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                        <Setter Property="Background" Value="#1E293B"/>
                                        <Setter Property="BorderBrush" Value="#334155"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Grid.Row="0" Margin="0,0,0,8">
                                <TextBlock Text="{Binding SelectedSheetResult.SheetName, StringFormat=📄 Chi tiết trang: {0}}" FontWeight="Bold" FontSize="12.5">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#0F172A"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                                    <Setter Property="Foreground" Value="#F8FAFC"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <TextBlock Text="{Binding SelectedSheetResult.SummaryText}" FontSize="11" Foreground="#64748B" Margin="0,2,0,0"/>
                            </StackPanel>

                            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding SelectedSheetResult.Pages}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border BorderThickness="1" CornerRadius="4" Padding="8,6" Margin="0,0,0,6">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#F8FAFC"/>
                                                        <Setter Property="BorderBrush" Value="#E2E8F0"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsWorkPage}" Value="True">
                                                                <Setter Property="Background" Value="#F0FDF4"/>
                                                                <Setter Property="BorderBrush" Value="#BBF7D0"/>
                                                            </DataTrigger>
                                                            <DataTrigger Binding="{Binding Status}" Value="BlankPage">
                                                                <Setter Property="Opacity" Value="0.6"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <Grid>
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>

                                                    <Border Grid.Column="0" Width="22" Height="22" CornerRadius="11" Margin="0,0,8,0" VerticalAlignment="Center">
                                                        <Border.Style>
                                                            <Style TargetType="Border">
                                                                <Setter Property="Background" Value="#E2E8F0"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsWorkPage}" Value="True">
                                                                        <Setter Property="Background" Value="#22C55E"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Border.Style>
                                                        <TextBlock Text="{Binding PageNumber}" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Setter Property="Foreground" Value="#475569"/>
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsWorkPage}" Value="True">
                                                                            <Setter Property="Foreground" Value="#FFFFFF"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                    </Border>

                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                        <TextBlock Text="{Binding RangeAddress, StringFormat=Vùng in: {0}}" FontSize="11" FontWeight="SemiBold" Foreground="#1E293B"/>
                                                        <TextBlock Text="{Binding Description}" FontSize="10.5" Foreground="#64748B" Margin="0,1,0,0"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Grid>

        <!-- ==================== 3. FOOTER BAR ==================== -->
        <Border Grid.Row="3" BorderThickness="0,1,0,0" Padding="20,12">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Background" Value="#FFFFFF"/>
                    <Setter Property="BorderBrush" Value="#E2E8F0"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                            <Setter Property="Background" Value="#1E293B"/>
                            <Setter Property="BorderBrush" Value="#334155"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{loc:Loc PageCounter_FooterTip}" FontSize="11.5" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#64748B"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDarkTheme, RelativeSource={RelativeSource AncestorType=Window}}" Value="True">
                                    <Setter Property="Foreground" Value="#94A3B8"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Command="{Binding OpenEvidenceWorkbookCommand}" Style="{StaticResource EvidenceActionBtn}" Margin="0,0,8,0" Visibility="{Binding HasEvidenceFile, Converter={StaticResource BoolToVisConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🎨" Margin="0,0,6,0"/>
                            <TextBlock Text="{loc:Loc PageCounter_BtnOpenEvidence}"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding ExportReportCommand}" Style="{StaticResource SuccessActionBtn}" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📊" Margin="0,0,6,0"/>
                            <TextBlock Text="{loc:Loc PageCounter_BtnExport}"/>
                        </StackPanel>
                    </Button>
                    <Button Content="{loc:Loc Common_Close}" Style="{StaticResource SecondaryActionBtn}" Click="BtnClose_Click" Width="80"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
